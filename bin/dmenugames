#!/bin/sh

favorites="\
duckstation-qt:/usr/bin/duckstation-qt
flycast:/usr/bin/flycast
pcsx2-qt:/usr/bin/pcsx2-qt"

choice=$(printf '%s\n' "$favorites" | cut -d':' -f1 | dmenu -l 20) || exit 1

"$choice"
