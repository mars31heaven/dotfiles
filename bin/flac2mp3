#!/bin/sh

fd -t f -e flac -x ffmpeg -i "{}" -c:a libmp3lame -b:a 320k "{.}.mp3"
fd -t f -e wav  -x ffmpeg -i "{}" -c:a libmp3lame -b:a 320k "{.}.mp3"

fd -t f -e flac -x sh -c '[ -f "{.}.mp3" ] && rm "{}"'
fd -t f -e wav  -x sh -c '[ -f "{.}.mp3" ] && rm "{}"'
