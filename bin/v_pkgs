#!/bin/sh
# Prints a list of every currently installed package on a Void Linux system
echo "$(uname -a | awk '{ print }' && xbps-query -l | awk '{ print $2 }' | xargs -n1 xbps-uhelper getpkgname)" > ~/.local/share/void_packages
